# 这是项目总的配置文件, 会作为环境变量加载到各个容器中
# 格式必须是 KEY=VALUE 不能有空格等

# Compose项目设置
TAG=dev
COMPOSE_PROJECT_NAME=jms
DOCKER_SUBNET=192.168.250.0/24

## 持久化目录, 安装启动后不能再修改, 除非移动原来的持久化到新的位置
VOLUME_DIR=/opt/fit2openshift

# Nginx 配置，这个Nginx是用来分发路径到不同的服务
HTTP_PORT=8080
HTTPS_PORT=8443

# Api 配置
SECRET_KEY=CW5kNiBvcHRpb25zPTIwMTxQRVJGT1JNTlVELERBRD4Kdm1uZ

# MySQL数据库配置
DB_ENGINE=mysql
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=BvcHRpb25zPTIwMTxQRV
DB_NAME=jumpserver

# Redis配置
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=jfjuU4O4xMLX


# Mysql 容器启动
MYSQL_ROOT_PASSWORD=BvcHRpb25zPTIwMTxQRV
MYSQL_DATABASE=jumpserver