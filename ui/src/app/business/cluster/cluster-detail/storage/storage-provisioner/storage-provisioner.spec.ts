import { StorageProvisioner } from './storage-provisioner';

describe('StorageProvisioner', () => {
  it('should create an instance', () => {
    expect(new StorageProvisioner()).toBeTruthy();
  });
});
