import { MessageTypePipe } from './message-type.pipe';

describe('MessageTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MessageTypePipe();
    expect(pipe).toBeTruthy();
  });
});
