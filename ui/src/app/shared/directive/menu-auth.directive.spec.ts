import { MenuAuthDirective } from './menu-auth.directive';

describe('MenuAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuAuthDirective();
    expect(directive).toBeTruthy();
  });
});
