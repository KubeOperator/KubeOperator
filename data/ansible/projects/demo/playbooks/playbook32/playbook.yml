- hosts: all
  roles: []
  tasks:
  - name: Update use key
    user: user=web state=present
  - name: ping
    ping: ''
  vars:
    additionalProp1: string
    additionalProp2: string
    additionalProp3: string
