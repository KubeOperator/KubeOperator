{
  "CN": "kubernetes",
  "hosts": [
    "127.0.0.1",
    {% for h in groups['cluster_nodes'] %}
    "{{hostvars[h]['ansible_ssh_host']}}",
    "{{hostvars[h]['inventory_hostname']}}",
    {% endfor %}
    "{{ CLUSTER_KUBERNETES_SVC_IP }}",
    "kubernetes",
    "kubernetes.default",
    "kubernetes.default.svc",
    "kubernetes.default.svc.cluster",
    "kubernetes.default.svc.cluster.local"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "CN",
      "ST": "BEIJING",
      "L": "XS",
      "O": "k8s",
      "OU": "System"
    }
  ]
}
